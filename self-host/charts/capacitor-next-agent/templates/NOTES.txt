Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

Capacitor Agent has been deployed successfully!

To check the agent status:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "capacitor-agent.name" . }} --tail=50

For more information, visit: https://github.com/gimlet-io/capacitor
